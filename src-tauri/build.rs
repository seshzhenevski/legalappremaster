// build.rs
// Скрипт сборки Tauri.
//
// Запускается перед компиляцией и генерирует служебный код Tauri
// (обработку иконок, конфигурации и т.д.).

/// Точка входа скрипта сборки.
///
/// Передаёт управление tauri_build, который выполняет всю подготовку
/// перед компиляцией основного приложения.
fn main() {
    tauri_build::build();
}
