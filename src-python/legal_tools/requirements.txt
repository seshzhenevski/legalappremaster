# Зависимости пакета legal_tools. Актуальный список для установки —
# src-python/requirements.txt (там же google-auth и requests для дашборда).
# PySide6 удалён 20.07.2026: Qt-слоя ui/ в проекте не существует.
# TODO: сверить список при первом запуске на машине с установленным Python.
python-docx>=1.1
reportlab>=4.0
Pillow>=10.0
num2words>=0.5
openpyxl>=3.1
pypdf>=4.0
