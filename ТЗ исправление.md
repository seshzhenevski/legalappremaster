# ТЗ: интерфейс и функциональность вкладок

Документ описывает эталонный интерфейс из настольной версии
(`legal_tools_app.py`, PySide6) — по нему нужно доработать текущий
Tauri-порт, где функционал вышел неполным. Для каждой вкладки указано:
из каких блоков она состоит, какие поля содержит, что делает каждая кнопка
и какая логика стоит за ней.

Общая компоновка окна: четыре вкладки шириной на все окно (1320×820).
Три из четырёх вкладок используют раскладку «две колонки»: слева форма
ввода, справа лог/результат.

---

## Вкладка 1. Генератор искового заявления

**Назначение:** по реквизитам ответчика и Excel-реестру счетов формирует
пакет из трёх документов (исковое заявление, опись вложения, платёжное
поручение на госпошлину) и находит подтверждающие PDF-документы.

**Раскладка:** две колонки. Слева — прокручиваемая форма из карточек,
внизу неподвижная панель действий. Справа — лог процесса.

### Левая колонка

**Карточка «🏢 Ответчик»**
- Поле «Наименование» (текст, плейсхолдер «ООО «Ответчик»»).
- Строка «ИНН» — поле ввода (плейсхолдер «10 или 12 цифр») + кнопка
  **«🔍 Найти»** справа. Кнопка подтягивает реквизиты из ЕГРЮЛ через DaData.
- Поле «ОГРН».
- Поле «Адрес».

Логика кнопки «🔍 Найти»: проверяет корректность ИНН (10 или 12 цифр),
запускает фоновый запрос к DaData, на время запроса показывает «⏳...».
При успехе автозаполняет Наименование, ОГРН, Адрес и дублирует ИНН, пишет
в лог «✅ Данные по ИНН … подтянуты из ЕГРЮЛ». При отсутствии ключа
DaData — открывает **диалог настройки ключа** (см. ниже). При ошибке —
показывает предупреждение.

**Диалог настройки ключа DaData** (модальное окно):
- Пояснение, что для поиска нужен бесплатный ключ DaData, со ссылкой на
  dadata.ru и краткой инструкцией получения (3 шага).
- Поле ввода «API-ключ» (подставляет уже сохранённый ключ, если есть).
- Кнопки «Сохранить и найти» и «Отмена». После сохранения ключ
  запоминается и поиск повторяется автоматически.

**Карточка «📨 Претензия»**
- Поле «Номер претензии» (текст).
- Поле «Дата претензии» (выбор даты, по умолчанию сегодня).

**Карточка «💰 Финансы»**
- Поле «Почтовые расходы» (число, плейсхолдер «0,00») + подпись «руб.».
- Строка ставки неустойки: «Процент неустойки» [поле, по умолч. «0.1»]
  «% в день до» [дата окончания периода] «, не более» [поле] «%».
  Последнее поле — необязательное ограничение (капинг) неустойки в процентах
  от суммы долга.

**Карточка «📂 Файлы»**
- Подпись-пояснение «Данные о счетах (для поиска подтверждающих документов)».
- Многострочное текстовое поле для вставки описания счетов из 1С
  (с примером-плейсхолдером формата «Счет на оплату № … / 1. Акт … / 2. УПД …»).
- Строка «Excel со счетами» — поле пути + кнопка «Обзор» (фильтр
  *.xlsx *.xlsm *.xls).
- Строка «Папка с закрывающими документами» — поле + «Обзор» (выбор папки).
- Строка «Папка сохранения результата» — поле + «Обзор» (выбор папки).
- Чекбокс «Закрывающие документы подписаны» (по умолчанию включён) —
  влияет на формулировку в тексте иска.

**Панель «⚙️ Действия»** (неподвижна внизу)
- Крупная кнопка **«📝 Сформировать иск»** — запускает генерацию.
- Кнопка «Очистить» — сбрасывает все поля формы.

### Правая колонка

**Карточка «📊 Процесс формирования»**
- Большое поле лога (только для чтения), куда построчно пишется ход работы;
  предупреждения выделяются красным жирным.
- Кнопка «Очистить лог».
- Тонкая полоса прогресса (появляется во время работы).

### Что происходит по «Сформировать иск»
Читается Excel-реестр, счета группируются с FIFO-распределением возвратов,
считается неустойка по каждому счёту и госпошлина, затем создаются три файла
в папке результата (Исковое заявление.docx, Опись.docx, Госпошлина.pdf) и
собираются найденные подтверждающие PDF. Ход и итоги пишутся в лог.

> **Расхождение с текущим портом:** в Tauri-версии сейчас нет поля
> «Папка с закрывающими документами», нет многострочного поля описания
> счетов, нет диалога настройки ключа DaData (ключ спрашивается через prompt),
> нет лога с прогрессом (только строка статуса).

---

## Вкладка 2. Сортировщик документов

**Назначение:** из папки с PDF-файлами собирает пакеты «Счёт + Акт + УПД»
в отдельные объединённые PDF по описанию из 1С.

**Раскладка:** две колонки. Слева — шаги ввода, справа — процесс и статистика.

### Левая колонка

**Карточка «📂 Шаг 1: Выбор папок»**
- Строка «Папка с исходными PDF» — поле + кнопка «Обзор».
- Строка «Папка для результата» — поле + кнопка «Обзор».

**Карточка «📋 Шаг 2: Данные о счетах»**
- Подпись «Вставьте информацию из 1С (Ctrl+V)».
- Кнопка «Загрузить из файла» — подгружает описание из текстового файла.
- Многострочное поле для вставки описания счетов (плейсхолдер с примером
  на два счёта).

**Карточка «⚙️ Настройки и запуск»**
- Чекбокс «Проверять целостность пакетов» — предупреждает, если для счёта
  найдены не все документы.
- Чекбокс «Открыть папку после обработки».
- Крупная кнопка **«▶ НАЧАТЬ ОБРАБОТКУ»**.
- Кнопки «Очистить» и «Справка» (справка — модальное окно с инструкцией
  и описанием формата данных).

### Правая колонка

**Карточка «📊 Процесс обработки»**
- Поле лога (только для чтения), предупреждения — красным.
- Кнопка «Очистить лог».
- Полоса прогресса с плавной анимацией.
- **Блок статистики** из трёх счётчиков: «Обработано», «Успешно»,
  «Предупреждений».

### Что происходит по «Начать обработку»
Из описания парсятся счета с их актами/УПД, в папке-источнике ищутся
соответствующие PDF, для каждого счёта документы объединяются в один PDF
в порядке Счёт → Акт → УПД. Ведётся статистика и лог; при включённой
проверке целостности отмечаются неполные пакеты.

> **Расхождение с текущим портом:** в Tauri-версии нет кнопки «Загрузить
> из файла», нет чекбоксов «Проверять целостность» и «Открыть папку после»,
> нет блока статистики (3 счётчика), нет кнопки «Справка», нет анимированного
> прогресса.

---

## Вкладка 3. Калькулятор неустойки

**Назначение:** расчёт договорной неустойки по одной или нескольким
задолженностям с учётом частичных оплат и опционального ограничения.

**Раскладка:** две колонки (слева форма ~40%, справа результат ~60%).

### Левая колонка (прокручиваемая)

**Карточка «💰 Сумма задолженности и период»**
- Поле «Сумма задолженности» (плейсхолдер «0,00») + «рублей».
- Поле «Дата начала просрочки» (выбор даты).
- Поле «Дата окончания периода» (выбор даты).

**Карточка «📈 Ставка неустойки»**
- Поле ставки (по умолч. «0.1») + «%» + выпадающий список **«в день / в год»**
  (тип ставки: дневная или годовая — при годовой формула делится на 365).
- «, но не более» [поле] «%» — необязательное ограничение неустойки.

**Карточка «➕ Дополнительные задолженности»**
- Кнопка «+ Добавить задолженность» — добавляет строку (сумма + дата начала).
  Позволяет считать неустойку сразу по нескольким долгам.

**Карточка «➖ Частичная оплата»**
- Кнопка «− Добавить оплату» — добавляет строку платежа (сумма + дата).
  Оплаты распределяются по долгам и уменьшают базу расчёта со следующего дня.

**Панель «⚙️ Действия»** (неподвижна внизу)
- Кнопка **«📂 Загрузить из Excel»** — импорт задолженностей и оплат
  из Excel-файла.
- Кнопка **«▶ Рассчитать»**.
- Кнопка «Очистить».

### Правая колонка

**Карточка «📊 Результат расчёта»**
- Кнопка «📋 Копировать таблицу» — копирует результат в буфер обмена.
- Строки итогов: «Сумма долга: …» и «Неустойка: …».
- **Таблица результата** из 8 столбцов: «Месяц», «Начислено», «Долг»,
  «С», «По», «Дней», «Формула», «Пени». Показывает пошаговый расчёт
  по периодам с формулами.

### Что происходит по «Рассчитать»
Считается календарное число дней в каждом подпериоде, оплаты уменьшают
базу, применяется дневная или годовая ставка, при заданном капинге
неустойка ограничивается. Результат раскладывается по строкам таблицы,
внизу — итоговые суммы.

> **Расхождение с текущим портом:** в Tauri-версии нет выбора «в день / в год»
> (только дневная), нет поля ограничения «не более … %», нет импорта из Excel,
> нет детальной таблицы расчёта (8 столбцов) и кнопки «Копировать таблицу» —
> показываются только итоговые две суммы.

---

## Вкладка 4. Калькулятор госпошлины

**Назначение:** расчёт госпошлины по цене иска (ст. 333.21 НК РФ) и
формирование платёжного поручения PDF.

**Раскладка:** две колонки (ввод слева, результат справа), панель действий внизу.

### Левая колонка

**Карточка «🏛 Стороны и сумма иска»**
- Пояснение мелким шрифтом (реквизиты плательщика/получателя фиксированы).
- Поле «Сумма иска» (плейсхолдер «например: 1 250 000») + «руб.».
- Поле «Ответчик» (плейсхолдер «ООО «Ответчик»»).
- Подпись-пояснение.

### Правая колонка

**Карточка «🧾 Госпошлина»**
- Поле «Госпошлина» — **редактируемое**: показывает рассчитанную сумму,
  но её можно поправить вручную или ввести без расчёта.
- Поле «Дата платежа» (выбор даты, по умолчанию сегодня).
- Подпись-пояснение, что поле остаётся редактируемым.

### Панель «⚙️ Действия» (внизу)
- Кнопка **«▶ Рассчитать»** — считает пошлину по сумме иска.
- Кнопка **«📄 Сформировать PDF»** — создаёт платёжное поручение.
- Кнопка «Очистить».

### Что происходит
«Рассчитать» применяет шкалу ст. 333.21 НК РФ и подставляет сумму в поле
(с округлением копеек). «Сформировать PDF» создаёт платёжное поручение
по форме, близкой к ОКУД 0401060, с фиксированными реквизитами получателя
(Казначейство/АСГМ) и плательщика, суммой цифрами и прописью.

> **Расхождение с текущим портом:** в Tauri-версии нет поля «Ответчик»,
> нет поля «Дата платежа», нет возможности отредактировать сумму пошлины
> вручную, и главное — **нет кнопки «Сформировать PDF»**: платёжка сейчас
> создаётся только внутри «Генератора иска», а отдельно на этой вкладке — нет.
> Показывается только рассчитанная сумма и пропись.

---

## Сводка расхождений (что доработать в Tauri-порте)

| Вкладка | Чего не хватает в текущем порте |
|---------|--------------------------------|
| **Генератор иска** | папка с закрывающими документами; многострочное поле описания счетов; диалог настройки ключа DaData; лог с прогресс-баром |
| **Сортировщик** | «Загрузить из файла»; чекбоксы «Проверять целостность» и «Открыть папку»; блок статистики (3 счётчика); «Справка»; анимация прогресса |
| **Неустойка** | переключатель «в день / в год»; поле ограничения «не более … %»; импорт из Excel; детальная таблица (8 столбцов); «Копировать таблицу» |
| **Госпошлина** | поле «Ответчик»; поле «Дата платежа»; ручная правка суммы; **кнопка «Сформировать PDF»** |

Приоритет по важности функционала: генерация PDF-платёжки на вкладке
госпошлины и детальная таблица неустойки — самые заметные пропажи, за ними
импорт из Excel (неустойка) и поиск закрывающих документов (иск).

---

# Часть 2. Полная сверка функций монолит → порт

Результат построчной сверки всех обработчиков каждой вкладки исходного
монолита с текущим Tauri-портом: перенесено (✅), частично (⚠️), нет (❌).

## Генератор искового заявления

| Функция монолита | Назначение | Статус в порте |
|------------------|-----------|----------------|
| Поиск по ИНН (DaData) | автозаполнение реквизитов | ✅ есть |
| Диалог настройки ключа DaData | ввод и сохранение ключа | ❌ заменён на prompt, ключ не сохраняется между сессиями |
| Поля Наименование / ИНН / ОГРН / Адрес | реквизиты ответчика | ✅ есть |
| Номер претензии + Дата претензии | данные претензии | ✅ есть |
| Почтовые расходы | сумма расходов | ✅ есть |
| Ставка + «до даты» + «не более %» | параметры неустойки | ✅ есть |
| Многострочное поле описания счетов | текст из 1С для поиска документов | ❌ отсутствует |
| Выбор Excel со счетами | путь к реестру | ✅ есть |
| Папка с закрывающими документами | источник PDF | ❌ отсутствует |
| Папка результата | куда сохранять | ✅ есть |
| Чекбокс «документы подписаны» | формулировка иска | ✅ есть |
| Кнопка «Сформировать иск» | генерация пакета | ✅ есть |
| Кнопка «Очистить» | сброс формы | ❌ отсутствует |
| Лог процесса + прогресс-бар | ход работы | ⚠️ только строка статуса |

## Сортировщик документов

| Функция монолита | Назначение | Статус в порте |
|------------------|-----------|----------------|
| Выбор папок источника и результата | пути | ✅ есть |
| Многострочное поле описания счетов | данные из 1С | ✅ есть |
| Кнопка «Загрузить из файла» | подгрузка описания из txt | ❌ отсутствует |
| Чекбокс «Проверять целостность пакетов» | предупреждать о неполных | ❌ отсутствует |
| Чекбокс «Открыть папку после обработки» | автооткрытие результата | ❌ отсутствует |
| Кнопка «Начать обработку» | запуск сборки | ✅ есть |
| Кнопка «Очистить» | сброс | ❌ отсутствует |
| Кнопка «Справка» | окно с инструкцией | ❌ отсутствует |
| Лог + анимированный прогресс | ход работы | ⚠️ лог есть, прогресса нет |
| Статистика: Обработано / Успешно / Предупреждений | 3 счётчика | ❌ отсутствует |

## Калькулятор неустойки

| Функция монолита | Назначение | Статус в порте |
|------------------|-----------|----------------|
| Сумма долга + дата начала + дата окончания | основной период | ✅ есть |
| Ставка + «%» | ставка неустойки | ✅ есть |
| Переключатель «в день / в год» | тип ставки (годовая ÷365) | ❌ отсутствует |
| Ограничение «но не более … %» | капинг неустойки | ❌ отсутствует |
| «+ Добавить задолженность» | несколько долгов | ✅ есть |
| «− Добавить оплату» | частичные оплаты (FIFO) | ✅ есть |
| Кнопка «Загрузить из Excel» | импорт долгов и оплат | ❌ отсутствует |
| Кнопка «Рассчитать» | расчёт | ✅ есть |
| Кнопка «Очистить» | сброс | ❌ отсутствует |
| Итоги: сумма долга + неустойка | результат | ✅ есть |
| Таблица расчёта (8 столбцов) | пошаговый расчёт по периодам | ❌ отсутствует (бэкенд `blocks` уже возвращает) |
| «Копировать таблицу» (HTML+TSV) | экспорт в буфер | ❌ отсутствует |

## Калькулятор госпошлины

| Функция монолита | Назначение | Статус в порте |
|------------------|-----------|----------------|
| Поле «Сумма иска» | цена иска | ✅ есть |
| Поле «Ответчик» | наименование для платёжки | ❌ отсутствует |
| Кнопка «Рассчитать» | расчёт пошлины | ✅ есть |
| Поле «Госпошлина» (редактируемое) | сумма с ручной правкой | ⚠️ показывается, не редактируется |
| Поле «Дата платежа» | дата для платёжки | ❌ отсутствует |
| Кнопка «Сформировать PDF» | платёжное поручение | ❌ отсутствует (главная пропажа) |
| Кнопка «Очистить» | сброс | ❌ отсутствует |
| Результат: сумма + прописью | вывод | ✅ есть |

## Вывод сверки

Расчётное **ядро перенесено полностью и корректно** (контрольный расчёт
ГИТИ совпадает до копейки). Все пропажи — в **слое интерфейса и RPC**:
формы беднее оригинала. Ни одна пропажа не требует переписывания логики —
только достройку интерфейса и местами тонкий RPC-метод поверх готовых
функций ядра. Сквозная деталь: **кнопка «Очистить» отсутствует на всех
четырёх вкладках**.

---

# Часть 3. Обнаруженные дефекты

## 3.1. Кракозябры при кириллице (`��� �������� �һ`)

**Причина:** Python-sidecar пишет ответы с русскими буквами
(`ensure_ascii=False`), но на Windows stdout по умолчанию в кодировке
системы (cp1251), а Rust читает байты как UTF-8 — кодировки не совпадают.
На Linux бага нет, поэтому в тестах не проявилась.

**Решение:** в начале `main.py` принудительно перевести потоки в UTF-8:

```python
import sys, io
sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8", newline="")
sys.stdin = io.TextIOWrapper(sys.stdin.buffer, encoding="utf-8")
```

**Приоритет: критический** — без этого интерфейс нечитаем.

## 3.2. Календарь: минималистичный стиль

**Требование:** современный минималистичный вид полей даты, убрать
постоянную подсветку текущей даты и месяца.

**Реализация:** поля дат — HTML `<input type="date">`, календарь рисует
WebView2. Либо стилизовать псевдоэлементы (`::-webkit-calendar-picker-indicator`)
через CSS под общий стиль, либо подключить лёгкий кастомный date-picker на
чистом JS с оформлением под Tailwind. Текущую дату показывать нейтрально,
без специальной подсветки. **Приоритет: средний.**

## 3.3. Полные названия вкладок

| Сейчас | Должно быть |
|--------|-------------|
| 📝 Генератор иска | 📝 Генератор искового заявления |
| 📁 Сортировщик | 📁 Сортировщик документов |
| 💰 Неустойка | 💰 Калькулятор неустойки |
| 🏛 Госпошлина | 🏛 Калькулятор госпошлины |

**Реализация:** заменить текст в кнопках `<nav>` в `index.html`.
**Приоритет: низкий.**

## 3.4. Drag-and-drop для Excel

**Требование:** в области выбора Excel со счетами разрешить перетаскивание
файла мышью, а не только «Обзор».

**Реализация:** Tauri 2 принимает перетаскиваемые файлы через
`onDragDropEvent` из `getCurrentWebview()`. Нужно: оформить зону сброса
(пунктирная рамка, подпись «Перетащите Excel-файл сюда или нажмите Обзор»);
при сбросе проверить расширение `.xlsx/.xls` и подставить путь; подсвечивать
зону при наведении. Нужна минимум в двух местах: Excel в «Генераторе иска»
и импорт в «Калькуляторе неустойки». **Приоритет: средний.**

---

# Часть 4. Сводный план доработки по приоритетам

**Критический:**
1. Фикс кодировки UTF-8 (кракозябры).

**Высокий (функциональные пропажи):**
2. Госпошлина: поле «Ответчик», «Дата платежа», редактируемая сумма, «Сформировать PDF».
3. Неустойка: таблица (8 столбцов) + «Копировать таблицу».
4. Неустойка: переключатель «в день / в год» + ограничение «не более %».
5. Неустойка + Иск: импорт из Excel и папка закрывающих документов.

**Средний:**
6. Кнопки «Очистить» на всех вкладках.
7. Лог с прогресс-баром (Иск, Сортировщик) + статистика (Сортировщик).
8. Drag-and-drop для Excel.
9. Минималистичный календарь.

**Низкий:**
10. Полные названия вкладок.
11. Сортировщик: «Загрузить из файла», чекбоксы, «Справка».
12. Диалог настройки ключа DaData вместо prompt.
