/** @type {import('tailwindcss').Config} */
// Дизайн-система «графит на белом» (design_new/DESIGN.md) с акцентом #0055FE.
// Классы в разметке остаются прежними (slate/blue) — здесь переопределены их
// ЗНАЧЕНИЯ под новые токены. Так вся перекраска живёт в одном месте, а
// структура вёрстки не тронута.
module.exports = {
  // Библиотеки из vendor/ исключены: сканировать минифицированный Chart.js
  // незачем — своих классов там нет, а строки из его кода Tailwind принимает
  // за имена классов и раздувает сборку.
  content: ["./src/**/*.{html,js}", "!./src/js/vendor/**"],
  theme: {
    extend: {
      colors: {
        // Серый ряд → токены референса: mist / veil / edge / hollow / mid-ash / graphite
        slate: {
          50: "#f9f9f9",   // Sidebar Mist — фон панелей и подложек
          100: "#f2f2f2",  // Hover Veil на белом (5% чёрного)
          200: "#e6e6e6",  // Edge Gray — hairline-границы карточек
          300: "#e0e0e0",  // граница форм (чуть заметнее hairline)
          400: "#8f8f8f",  // Hollow — третичный текст
          500: "#5d5d5d",  // Mid Ash — вторичный текст, подписи
          600: "#5d5d5d",  // Mid Ash
          700: "#0d0d0d",  // Graphite Ink — заголовки и тёмные кнопки
          800: "#0d0d0d",  // Graphite Ink
          900: "#0d0d0d",  // Graphite Ink
        },
        // Акцент приложения — #0055FE (переопределение «синего» ряда)
        blue: {
          50: "#f0f5ff",   // акцентная подложка (active-состояния)
          100: "#e0ebff",
          600: "#0055FE",  // основной акцент: CTA, ссылки, активные пункты
          700: "#0043c9",  // hover акцента
        },
      },
      // Радиусы референса: контролы и карточки 10px, крупные поверхности 16px
      borderRadius: {
        lg: "10px",
        xl: "10px",
        "2xl": "16px",
      },
    },
  },
  plugins: [],
};
